rules_version = '2';

// 모든 클라이언트 직결을 전면 차단한다.
// 데이터는 서버(Next.js)의 Admin 경로로만 접근하며, Admin SDK는 규칙을 우회한다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
