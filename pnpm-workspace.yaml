# pnpm: allow these dependencies to run install/build scripts (native binaries).
# Required for `next build` (sharp = image optimization) under pnpm, locally and on Vercel.
allowBuilds:
  '@firebase/util': true
  protobufjs: true
  sharp: true
  unrs-resolver: true
